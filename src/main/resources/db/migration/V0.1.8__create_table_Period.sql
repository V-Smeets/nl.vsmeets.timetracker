CREATE TABLE "PERIOD" (
	"PERIOD_ID" INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL CONSTRAINT "PERIOD_PK" PRIMARY KEY,
	"TASK_ID" INTEGER NOT NULL,
	CONSTRAINT "PERIOD_FK_TASK" FOREIGN KEY("TASK_ID") REFERENCES "TASK",
	"START_TIME" TIME NOT NULL,
	"END_TIME" TIME NOT NULL);