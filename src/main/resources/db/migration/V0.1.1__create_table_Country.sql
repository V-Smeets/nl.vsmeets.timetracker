CREATE TABLE "COUNTRY" (
	"COUNTRY_ID" INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL CONSTRAINT "COUNTRY_PK" PRIMARY KEY,
	"CODE" VARCHAR(2) NOT NULL CONSTRAINT "COUNTRY_UNQ_CODE" UNIQUE);