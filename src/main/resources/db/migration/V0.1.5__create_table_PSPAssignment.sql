CREATE TABLE "PSP_ASSIGNMENT" (
	"PSP_ASSIGNMENT_ID" INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL CONSTRAINT "PSP_ASSIGNMENT_PK" PRIMARY KEY,
	"PSP_ID" INTEGER NOT NULL,
	CONSTRAINT "PSP_ASSIGNMENT_FK_PSP" FOREIGN KEY("PSP_ID") REFERENCES "PSP",
	"USER_ID" INTEGER NOT NULL,
	CONSTRAINT "PSP_ASSIGNMENT_FK_USER" FOREIGN KEY("USER_ID") REFERENCES "USER",
	"START_DATE" DATE NOT NULL,
	"END_DATE" DATE NOT NULL);