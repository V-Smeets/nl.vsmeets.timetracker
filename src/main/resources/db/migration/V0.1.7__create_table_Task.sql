CREATE TABLE "TASK" (
	"TASK_ID" INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL CONSTRAINT "TASK_PK" PRIMARY KEY,
	"NAME" VARCHAR(32) NOT NULL,
	"PSP_ASSIGNMENT_ID" INTEGER,
	CONSTRAINT "TASK_FK_PSP_ASSIGNMENT" FOREIGN KEY("PSP_ASSIGNMENT_ID") REFERENCES "PSP_ASSIGNMENT",
	"PARENT_TASK_ID" INTEGER,
	CONSTRAINT "TASK_FK_TASK" FOREIGN KEY("PARENT_TASK_ID") REFERENCES "TASK");