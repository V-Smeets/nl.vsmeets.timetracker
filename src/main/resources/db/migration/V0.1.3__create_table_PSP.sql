CREATE TABLE "PSP" (
	"PSP_ID" INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL CONSTRAINT "PSP_PK" PRIMARY KEY,
	"PROJECT_ID" INTEGER NOT NULL,
	CONSTRAINT "PSP_FK_PROJECT" FOREIGN KEY("PROJECT_ID") REFERENCES "PROJECT",
	"CODE" VARCHAR(3) NOT NULL,
	"NAME" VARCHAR(64));