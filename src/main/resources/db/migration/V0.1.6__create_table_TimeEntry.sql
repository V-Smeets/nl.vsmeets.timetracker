CREATE TABLE "TIME_ENTRY" (
	"TIME_ENTRY_ID" INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL CONSTRAINT "TIME_ENTRY_PK" PRIMARY KEY,
	"PSP_ASSIGNMENT_ID" INTEGER NOT NULL,
	CONSTRAINT "TIME_ENTRY_FK_PSP_ASSIGNMENT" FOREIGN KEY("PSP_ASSIGNMENT_ID") REFERENCES "PSP_ASSIGNMENT",
	"DATE" DATE NOT NULL,
	"DURATION" INTEGER NOT NULL,
	"COMMENT" VARCHAR(32));