create table "PSP" (
	"PSP_ID" INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,
	"PROJECT_ID" INTEGER NOT NULL,
	"CODE" VARCHAR NOT NULL,
	"NAME" VARCHAR NOT NULL);
alter table "PSP" add constraint "PSP_FK_PROJECT"
	foreign key("PROJECT_ID") references "PROJECT"("PROJECT_ID") on update NO ACTION on delete NO ACTION;