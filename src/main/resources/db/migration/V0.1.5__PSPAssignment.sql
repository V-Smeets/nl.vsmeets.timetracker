create table "PSP_ASSIGNMENT" (
	"PSP_ASSIGNMENT_ID" INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,
	"PSP_ID" INTEGER NOT NULL,
	"USER_ID" INTEGER NOT NULL,
	"START_DATE" DATE NOT NULL,
	"END_DATE" DATE NOT NULL);
alter table "PSP_ASSIGNMENT" add constraint "PSP_ASSIGNMENT_FK_PSP"
	foreign key("PSP_ID") references "PSP"("PSP_ID") on update NO ACTION on delete NO ACTION;
alter table "PSP_ASSIGNMENT" add constraint "PSP_ASSIGNMENT_FK_USER"
	foreign key("USER_ID") references "USER"("USER_ID") on update NO ACTION on delete NO ACTION;